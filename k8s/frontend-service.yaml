apiVersion: v1
kind: Service
metadata:
  name: portfolio-frontend-service
  namespace: portfolio
  labels:
    app: portfolio-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: portfolio-frontend
